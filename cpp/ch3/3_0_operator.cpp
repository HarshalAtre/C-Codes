#include <iostream>
using namespace std;
int main(){
    //Arithmatic operators

    //binary operators = +,-,x,%,/.
    //Unary operator = ++,--
    
    // Relational Operators
    //== equal to sign
    //!= not equal to sign
    //>,< more than or less than , compares value
    //>=,<= more than equal to or less than equal to 

    //Logical Operators
    //&& and
    //|| or
    //! not

    //Bit operators
    //& and
    //| or
    //^ XOR
    //~ ones(not)
    //<< Left Shift
    //>> Right Shift
    // formula :
    // a<<n ----> a*2^n
    // a>>n ----> a/2^n

    //Assignment operators(easy)
    //= equal to
    //+= example a += 3 means a=a+3 
    //-=
    //*=
    ///=
 
    // Miscelloneous operators
    // sizeof() --> tells us the size of the variable ex, sizeof(a)
    //condition?X:Y --> if condition is true exwcutes X else executes Y
    // cast --> to convert type to variable eg,int(ch)
    //comma --> causes sequence of operations to be performed eg, int a,b,c;
    //Note: if a=1,2,3 then last element which is 3 here will be assigned to a
    //reference(&)--> returns the address of a variable
    // pointer(*) --> PPointer To a Variable
    

                                          
    
    return 0;
}     