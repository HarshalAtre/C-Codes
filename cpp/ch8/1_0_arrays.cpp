#include <iostream>
using namespace std;
int main(){
    //Arrays 
    //arrays are the list of variables of similar type
    //{1,2,3,4}---> same type(int)
    //{"d",2,4.5}---> Not same type (not an array)
    //
    //Initializing the array
    //data-type arrayname [size];
    //Example
    int Name[4]={10,20,30,40};

    //The system allots memory to array according to datatype and size, int-->4bytes
    // so array---> 4x4=16bytes is reserved in memory
    return 0;
}